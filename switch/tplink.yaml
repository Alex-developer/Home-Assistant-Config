  # Alex's plug
  - platform: tplink
    host: !secret tplink_alex_plug

  # Rebecca's Plug
  - platform: tplink
    host: !secret tplink_becs_plug

  # Office Desk
  - platform: tplink
    host: !secret tplink_officedesk_plug

  # Racing PC
  - platform: tplink
    host: !secret tplink_racingpc_plug

  # Chargers
  - platform: tplink
    host: !secret tplink_chargers_plug

  # Pi Server
  - platform: tplink
    host: !secret tplink_pi_plug

  # Front Camera
  - platform: tplink
    host: !secret tplink_frontcamera_plug