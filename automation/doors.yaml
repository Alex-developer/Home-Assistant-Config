  - alias: 'Lounge Door Open'
    trigger:
      platform: state
      entity_id: sensor.back_door
      from: 'Closed'
      to: 'Open'
    action:
      service: notify.slackdoors
      data:
        message: "Lounge Door Is Open"
  - alias: 'Lounge Door Closed'
    trigger:
      platform: state
      entity_id: sensor.back_door
      from: 'Open'
      to: 'Closed'
    action:
      service: notify.slackdoors
      data:
        message: "Lounge Door Is Closed"

  - alias: 'Front Door Open'
    trigger:
      platform: state
      entity_id: sensor.front_door
      from: 'Closed'
      to: 'Open'
    action:
      service: notify.slackdoors
      data:
        message: "Front Door Is Open"
  - alias: 'Front Door Closed'
    trigger:
      platform: state
      entity_id: sensor.front_door
      from: 'Open'
      to: 'Closed'
    action:
      service: notify.slackdoors
      data:
        message: "Front Door Is Closed"

  - alias: 'Garage Door Open'
    trigger:
      platform: state
      entity_id: sensor.garage_door
      from: 'Closed'
      to: 'Open'
    action:
      service: notify.slackdoors
      data:
        message: "Garage Door Is Open"
  - alias: 'Garage Door Closed'
    trigger:
      platform: state
      entity_id: sensor.garage_door
      from: 'Open'
      to: 'Closed'
    action:
      service: notify.slackdoors
      data:
        message: "Garage Door Is Closed"