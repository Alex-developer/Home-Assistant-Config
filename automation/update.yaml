  - alias: 'Update Available Notifications'
    trigger:
      platform: state
      entity_id: updater.updater
    action:
    - service: notify.alex_notify
      data:
        message: "An Update Is Available For Home Assistant"